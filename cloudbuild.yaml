steps:
    # build the container image
  - name: 'asia.gcr.io/cloud-builders/docker'
    args: [ 'build', '-t', 'asia.gcr.io/$GCP_PROJECT_ID/irunning', '.' ]
    # push the container image
  - name: 'asia.gcr.io/cloud-builders/docker'
    args: [ 'push', 'asia.gcr.io/$GCP_PROJECT_ID/irunning']
    # deploy to Cloud Run
  #- name: "gcr.io/cloud-builders/gcloud"
  #  args: ['run', 'deploy', 'erp-ui', '--image', 'gcr.io/$PROJECT_ID/demo-app', '--region', 'europe-west4', '--platform', 'managed', '--allow-unauthenticated']
